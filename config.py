# The API key from https://accounts.gandi.net/
# Currently deprecated, and you should use access tokens.
api_key="<replace me to work>"
access_token="<replace me to work>"
domain="example.org"
subdomain="subdomain"
